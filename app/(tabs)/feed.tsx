import { View, Text } from 'react-native'
import { SafeAreaView } from 'react-native-safe-area-context'

export default function FeedScreen() {
  return (
    <SafeAreaView style={{ flex: 1, padding: 20 }}>
      <Text>Community Feed (coming soon)</Text>
    </SafeAreaView>
  )
}